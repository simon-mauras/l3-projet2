TODOLIST
- Dans le fichier dpll.ml : Renvoyer une solution dans le cas ou la formule est satisfiable.
                            Sous quelle forme ? Types.solution ?
- Dans le fichier main.ml : Afficher la solution renvoyée par le solver.
- Ajouter dans Formula.make un test pour supprimer les clauses triviales.
- Ajouter un fichier implémentant les watched literals (module de type Types.Formula_types)
- Harmoniser les noms de variables (nomDeVariables ou nom_de_variable ?)
